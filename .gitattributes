# Shell scripts and Dockerfiles must keep LF endings, or they break when the
# repository is cloned on Windows and the files are run inside a Linux container.
*.sh text eol=lf
Dockerfile* text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.conf text eol=lf
*.py text eol=lf
*.ps1 text eol=crlf
